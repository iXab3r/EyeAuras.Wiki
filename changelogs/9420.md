---
title: 1.9.9416
description: April update - Codex, Gateway and export follow-up
published: true
date: 2026-04-06T00:10:00.000Z
tags: ai, gateway, codex, export, ai-translated
editor: markdown
dateCreated: 2026-04-06T00:10:00.000Z
---
# 1.9.9416 [RU](/ru/changelogs/9416)/[EN](/en/changelogs/9416)

## Export improvements
The `Export` window has been improved. You can now configure pack options directly there, and also set the pack name and description.

This is still not the final version of the flow. The goal for the next two months is to get everything to a point where you can build a working mini-app in `10-15` minutes, have AI write it for you, upload it in one action, and immediately share it with anyone interested.

Right now, with the latest fixes included, we are roughly `70-80%` of the way there.

![Export Improvements](https://s3.eyeauras.net/media/2026/04/EyeAuras_eDtU1Z4Jye.png =x600)

## Update changelog improvements
I also improved the update window inside the app itself.

The changelog is now easier to navigate:
- you can switch between `RU` and `EN` patch notes directly in the update window
- links in the changelog were fixed, and should now open the correct wiki pages in your browser
- image rendering inside patch notes was improved

![Updater Changelog RU](https://s3.eyeauras.net/media/2026/04/EyeAuras_a8dtRZ1OV8.png =x400)
![Updater Changelog EN](https://s3.eyeauras.net/media/2026/04/EyeAuras_aJwIBiwzOU.png =x400)

## AI - Gateway
Added **free limits** to `EyeAuras AI Gateway`.

The idea is simple: everyone should have AI available right away, without needing to register an `OpenAI` account, add budget there, or pay for a subscription. This is especially important for users in Russia, where direct access to `OpenAI` is still blocked.

For most users, AI will probably be more of a reference tool: quickly clarify something, ask a one-off question, understand a topic, and move on. I hope that kind of usage will be fully covered by the free limits. For full-scale coding and serious scripting, it will most likely not be enough.

Important: AI is expensive. I reserve the right to keep adjusting the size of these limits, their availability, and the general usage rules as the feature starts seeing real load. The app is free, and I would still like at least _some_ AI access to be available to absolutely all users, so we need to find a balance.

## AI - EyeAuras Gateway
In simple terms, `EyeAuras Gateway` is a special server address that you can enter in the AI profile under **`OpenAI Endpoint`**, next to `API Key` and the rest of the settings.

From the user perspective, it is almost the same as a normal `OpenAI` profile, except that instead of the standard endpoint you use `EyeAuras Gateway`.

This is mainly useful for people who do **not** want to create their own `OpenAI` key, or who live in Russia. If you already have normal access to `OpenAI`, direct `OpenAI` is usually still the simpler option.

`EyeAuras Gateway` now supports two modes.

### Gateway
This is the main mode. You use AI as an EyeAuras user.

In this mode, you **do not need your own `OpenAI` account**. This is also how the free limits work: the `OpenAI` costs are handled on the EyeAuras side, and you simply use AI inside the app.

![EyeAuras Gateway](https://s3.eyeauras.net/media/2026/04/EyeAuras_DUoN1aOArT.png =x600)

### Proxy
This is the fallback mode. In this mode, `Gateway` does not pay for anything on your behalf. It simply forwards requests to `OpenAI` servers **using your own key**.

This is needed only for users in Russia.

In short:
- `Gateway` — you want to use AI without your own `OpenAI` account
- `Proxy` — you already have your own key, but need a convenient path to `OpenAI`

Important: in `Proxy` mode, your personal `OpenAI` key passes through EyeAuras servers. Technically, I tried to make this as safe as possible and avoid storing or logging anything unnecessary, but it is important to understand how the setup works.

[Read more about AI Assistant](/features/ai-assistant)

## AI - Codex
Added support for `Codex`, which is, in my opinion, one of the strongest AI assistants available right now.

The user flow is roughly the same as with regular `ChatGPT`: you give it a task, and the assistant tries to solve it using all tools available to it. The key difference is that `Codex` tends to dig much deeper into a problem, re-check its conclusions more often, and usually produces stronger results on difficult technical tasks.

Right now, `Codex` and `Claude Code` are pushing practical AI programming forward in a big way, and now we have that integration too.

For now, `Codex` is not running at full power yet: not all the wires are connected. For example, I intentionally have not connected it to the scripting system yet, even though that will likely be one of the most important future use cases and one of the main reasons it was added. Automatic behavior tree generation, script writing, self-diagnostics like "why is this not working" — these are exactly the scenarios where `Codex` is especially strong. But first I want to spend a week watching the quality of the integration itself, let it work as a reference tool for now, and then we can start giving it more freedom.

### Best use cases
The main area for `Codex` is complex tasks: programming, automatic configuration of auras, trees, macros, and similar systems.

Using it just as a reference tool is a bit like using a cannon to shoot sparrows. On top of that, this can simply be more expensive, because `Codex` tends to double-check itself. That is great for quality on hard tasks, but not very efficient for simple ones.

You can switch between `Codex` and regular `Chat` mode at any time, but conversation context is not shared between them.

p.s. The UI is still under active development, this is far from the final version.  
![EyeAuras Codex](https://s3.eyeauras.net/media/2026/04/EyeAuras_N9sVSOeQ5s.png =x600)

## Fixes and improvements
* **[UI]** Fixed a bug with the `StayOnTop` button in the window title bar
* **[UI]** Fixed an issue where `SubTree` could not be deleted in behavior trees